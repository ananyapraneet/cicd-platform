apiVersion: v1
kind: Service
metadata:
  name: {{ include "cicd-platform.fullname" . }}
  labels:
    {{- include "cicd-platform.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "cicd-platform.selectorLabels" . | nindent 4 }}
